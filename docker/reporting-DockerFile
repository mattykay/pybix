# Pybix Reporting debug container
#   This container sets up source code so that you can call Reporting code directly without setting up venv's etc.
#   built via `docker build -t pybix-debug -f reporting-DockerFile .` (if proxy --build-arg http_proxy=http://myproxy.com)
#   run via like `docker run pybix-debug <Python args>`
FROM python:3.7

COPY ../pybix/reporting /

RUN pip install -r requirements.txt
RUN pip install pybix

# Command arguments are passed when running `docker run`, simply call reporting script
ENTRYPOINT ["python", "reporting.py"]
CMD []
